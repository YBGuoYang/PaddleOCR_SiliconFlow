# Release Output

该目录用于存放本地打包产物。

当前产物类型：

- `ScreenshotOCR_Source_*.zip`：源码包（开发者）
- `ScreenshotOCR_Setup_*.exe`：安装包（普通用户，优先）
- `ScreenshotOCR_Portable_*.exe`：便携单文件（安装包回退）

注意：

- 可保留在本地用于分发。
- 不应提交二进制到 GitHub 仓库。

仓库中仅保留该说明文件和 `.gitkeep`。
